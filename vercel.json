{
  "buildCommand": "cd connector-api && npm run build",
  "outputDirectory": "connector-api/dist",
  "installCommand": "yarn install && cd connector-api && npm install",
  "env": {
    "NODE_ENV": "production",
    "ALLOWED_ORIGINS": "chrome-extension://*,moz-extension://*"
  },
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x",
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
