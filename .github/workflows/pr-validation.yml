name: PR Validation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guardrails:
    name: PR Guardrails
    runs-on: ubuntu-latest
    steps:
      - name: Ensure PR source is not main
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          if [ "${{ github.head_ref }}" = "main" ]; then
            echo "PRs must come from a feature branch, not main."
            exit 1
          fi
      - name: Require phase tag in PR title
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          title="${{ github.event.pull_request.title }}"
          echo "PR title: $title"
          echo "$title" | grep -E '\[(phase-[0-9]+|infra|docs|fix)\]' >/dev/null || {
            echo "PR title must include one tag: [phase-0], [phase-1], [infra], [docs], [fix]"
            exit 1
          }

  validate:
    name: Lint, Test, Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install && cd connector-api && npm install

      - name: Typecheck (extension + connector-api)
        run: yarn typecheck:all

      - name: Lint (extension + connector-api)
        run: yarn lint:all

      - name: Test (extension only)
        run: yarn test

      - name: Build (extension + connector-api)
        run: yarn build && cd connector-api && npm run build
